buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.3' // 여기에서 Android Gradle 플러그인 버전을 지정합니다.
        // 다른 종속성을 추가할 수 있습니다.
    }
}

plugins {
    id 'com.android.application' version '8.1.3' apply false
    id 'com.android.library' version '8.1.3' apply false
    id 'org.jetbrains.kotlin.android' version '1.7.20' apply false
}

// 다른 Android 프로젝트 설정...
